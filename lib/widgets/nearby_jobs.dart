import 'package:flutter/material.dart';

class NearbyJobsComponent extends StatelessWidget {
  const NearbyJobsComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Nearby jobs");
  }
}
